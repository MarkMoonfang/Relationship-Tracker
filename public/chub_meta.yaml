project_name: "Relationship Tracker"
tagline: "Track growing affection and trust with characters using visible, in-story logic."
visibility: 'PUBLIC'
position: 'ADJACENT'
tags:
  - 'relationship'
  - 'affection'
  - 'tracker'

# Optional configuration schema (if needed in your UI)
config_schema:
  type: object
  properties:
    relationshipLevel:
      type: integer
      default: 0

# Optional state schema (not required unless you're persisting or restoring state)
state_schema:
  init:
    type: object
    properties:
      relationshipLevel:
        type: integer
        default: 0

github_path: 'https://github.com/MarkMoonfang/Relationship-Tracker'


extension_id: 'relationship-tracker-3161b4156d42'

